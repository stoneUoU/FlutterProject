import 'package:flutter/material.dart';

class HiRouteCodeFooterWidget extends StatelessWidget {
  const HiRouteCodeFooterWidget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}