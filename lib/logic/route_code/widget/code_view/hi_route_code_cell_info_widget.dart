import 'package:flutter/material.dart';

class HiRouteCodeCellInfoWidget extends StatelessWidget {
  const HiRouteCodeCellInfoWidget({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}